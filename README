# GitHub as a Book - Parser Engine

A Python-based tool to analyze GitHub repositories and transform them into structured, readable documentation.

## Setup

1. Create virtual environment:
```python -m venv venv```

2. Activate virtual environment:
- Windows: ```venv\Scripts\activate```
- Unix/MacOS: ```source venv/bin/activate```

3. Install dependencies:
```pip install -e .[dev]```

## Development

TBD

## Testing

Run tests with:
```pytest```